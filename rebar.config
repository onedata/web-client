%% behaviours should be compiled before other files
{erl_first_files, [
    "src/websocket_client/websocket_client_handler_behaviour.erl"
]}.

{erl_opts, [
    fail_on_warning,
    {src_dirs, ["src"]},
    debug_info
]}.

%% eunit opts - Maven-like output formatting
{eunit_opts, [
    verbose,
    {report, {eunit_surefire, [{dir, "../test/eunit_results"}]}}
]}.

%% Test coverage
{cover_enabled, true}.

%% deps directory
{lib_dirs, ["deps"]}.

{deps, [
    {meck, "0.8.2", {git, "https://github.com/eproxus/meck.git", {tag, "0.8.2"}}},
    {hackney, ".*", {git, "git://github.com/benoitc/hackney.git", {tag, "1.3.x"}}},
    {ssl2, ".*", {git, "ssh://git@git.plgrid.pl:7999/vfs/erlang-tls.git", {tag, "eb76a02"}}}
]}.

%% Cleanup
{clean_files, ["test/eunit_results"]}.